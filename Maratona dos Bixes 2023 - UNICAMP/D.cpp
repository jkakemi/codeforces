#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
 
    int l, y; cin >> l >> y;

    int resultado = l | y;

    cout << resultado << endl;
    
    return 0;
}